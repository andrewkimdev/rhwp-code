---
kind: guide
status: active
canonical: mydocs/manual/agent_codex/90_미분류.md
last_verified: 2026-08-31
generated: tools/gen_agent_codex.py — 수기 수정 금지, 재생성으로 갱신
---

# 90 — 미분류 (가족 배정 대기)

> 새 명령이 가족 표에 아직 배정되지 않으면 여기 실린다 — 이 장이
> 비어 있지 않으면 FAMILIES 표를 갱신하라.

### `template-entity` — hwpx 표 역할 마커·누름틀에서 Java record 데이터/모듈 클래스 초안 생성(서버 없이)

- 종류: `query` · exit 규약: 0 성공 / 1 IO / 2 사용법 / 3 판정 실패(데이터)
- 플래그: `--code` · `--package` · `--out-dir` · `--json`
- 봉투 필드: `code` · `packageName` · `dataClassName` · `moduleClassName` · `dataClassSource` · `moduleClassSource` · `errors` — 정의는 [지식지도 §2-2](../agent_knowledge_map.md)

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.
